# TakeTime 产品定义

## 定位

TakeTime 是供一名用户自托管的每日用药记录与动态排程工具。它关注“现在下一步是什么”，并用当天真实作息、用餐时间和实际服药签到减少反复计算时间的负担。

产品不是固定闹钟表，也不是医疗决策系统。它不判断某种药是否适合用户，不生成处方、剂量、禁忌、相互作用或停药建议。

## 用户与范围

- 真实应用只有一名使用者，没有注册、组织、家庭成员或角色权限。
- 独立 Demo 面向公开展示，使用虚构数据并完全在浏览器内存运行。
- 当前不规划商业化、订阅、广告、多人共享或面向公众的托管服务。
- SQLite 是真实应用的持久化事实来源，运行模型是单实例、低并发、自托管。

## 核心流程

1. 用户设置常用起床、餐次和睡前时间。
2. 每天起床后主动确认实际起床时间，系统推算当天作息。
3. 首页显示最相关的下一项用药；完整计划仍可在时间线中查看。
4. 用户实际服药后主动签到。自动化只能计算和提醒，不能替用户确认已服用。
5. 撤销签到、调整当天作息或变更计划时，系统保留既往事实并重放尚未完成的安排。

## 三种时间模式

每种药只能选择一种时间依据：

- **跟随作息**：与起床或睡前等生活节奏相关。签到只形成记录，不推动同药后续剂次。
- **跟随用餐**：与早餐、午餐或晚餐相关。时间随餐次变化，签到不推动同药后续剂次。
- **跟随上次服用**：第一剂来自当天基础计划；之后根据前一次实际签到，保持同药相邻剂次的原定间隔。

间隔模式只影响当天同一种药尚未完成的后续剂次。单次偏移超出配置上限时只记录、不自动重排；已完成项目的计划时间不会被后续作息修改重写。

## 数据不变量

- 只有用户明确确认的行为能写入服药记录。
- 读取日期本身不得创建每日快照。
- 计划修改不得改写已经冻结的既往日期。
- 重复提交相同签到应保持幂等，不制造重复记录。
- 跨午夜项目保留原计划日归属，同时在实际执行日显示为跨日待办。
- Demo 不得读取、发送或修改 SQLite 数据。

## 医疗安全边界

- 所有用药名称、剂量、频率和允许偏移范围都必须来自用户已有医嘱或可靠说明，不能由产品猜测。
- “动态调整”只表达软件排程关系，不表示医学上允许改变服药时间。
- 越界时系统应明确说明没有自动调整，不能静默截断或假装安全。
- 文案保持平静、准确，不制造依从性焦虑，也不暗示疗效。
- 产品没有临床验证或医疗器械资质，不得编造医生背书、患者评价或疗效证据。

## 可访问性与界面原则

- 触控目标至少 44px，提供清晰键盘焦点。
- 重要状态同时使用文字与视觉形态，不能只依赖颜色。
- 支持 `prefers-reduced-motion`，动画只解释状态变化。
- 手机端保留完整功能，中文药名、剂量和说明允许长文本与系统字体缩放。
- 视觉基线见 [DESIGN_REQUIREMENTS.md](DESIGN_REQUIREMENTS.md)，医疗安全、可读性和操作效率优先。

## 明确不做

- 自动诊断、处方生成、剂量推荐或药物相互作用判断
- 未经用户确认自动标记已服药
- 多租户、商业收费、广告或销售医疗数据
- 把 Demo 与真实数据库、私人口令或真实药物方案连接起来
