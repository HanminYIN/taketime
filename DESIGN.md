---
name: "药时"
description: "以真实作息为锚点的安静、可靠个人用药操作界面"
colors:
  action-blue: "#0071e3"
  action-blue-hover: "#0077ed"
  link-blue: "#0066cc"
  dark-link-blue: "#2997ff"
  dark-link-blue-hover: "#62b3ff"
  ink: "#1d1d1f"
  text-secondary: "rgba(0, 0, 0, 0.62)"
  text-tertiary: "rgba(0, 0, 0, 0.56)"
  canvas-mist: "#f5f5f7"
  surface-white: "#ffffff"
  surface-muted: "#fafafc"
  night-glass: "rgba(18, 18, 20, 0.9)"
  light-glass: "rgba(255, 255, 255, 0.76)"
  recorded-green: "#1f7a35"
  recorded-surface: "#eef8f0"
  success-on-dark: "#72d58a"
  overdue-amber: "#8a5b00"
  danger-red: "#b42318"
  danger-surface: "#fff1f0"
  danger-on-dark: "#ffb4ad"
  divider: "rgba(0, 0, 0, 0.09)"
typography:
  brand-mark:
    fontFamily: '"Kaiti SC", STKaiti, KaiTi, serif'
    fontSize: "18px"
    fontWeight: 600
    lineHeight: 1
    letterSpacing: "0"
  brand-tagline:
    fontFamily: '"TakeTime Slogan Serif", "Songti SC", STSong, SimSun, serif'
    fontSize: "14px"
    fontWeight: 500
    lineHeight: 1.2
    letterSpacing: "0"
  metric-primary:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "52px"
    fontWeight: 600
    lineHeight: 1
    letterSpacing: "0"
  metric-history:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "46px"
    fontWeight: 600
    lineHeight: 1
    letterSpacing: "0"
  metric-primary-compact:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "44px"
    fontWeight: 600
    lineHeight: 1
    letterSpacing: "0"
  display:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "40px"
    fontWeight: 600
    lineHeight: 1.1
    letterSpacing: "0"
  metric-history-compact:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "38px"
    fontWeight: 600
    lineHeight: 1
    letterSpacing: "0"
  headline:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "34px"
    fontWeight: 600
    lineHeight: 1.14
    letterSpacing: "0"
  display-compact:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "32px"
    fontWeight: 600
    lineHeight: 1.1
    letterSpacing: "0"
  headline-compact:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "28px"
    fontWeight: 600
    lineHeight: 1.14
    letterSpacing: "0"
  headline-phone:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "26px"
    fontWeight: 600
    lineHeight: 1.14
    letterSpacing: "0"
  icon-dialog:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "25px"
    fontWeight: 300
    lineHeight: 1
    letterSpacing: "0"
  title:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "24px"
    fontWeight: 600
    lineHeight: 1.2
    letterSpacing: "0"
  icon-confirm:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "23px"
    fontWeight: 400
    lineHeight: 1
    letterSpacing: "0"
  title-compact:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Display", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "22px"
    fontWeight: 600
    lineHeight: 1.2
    letterSpacing: "0"
  medication-focus:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "18px"
    fontWeight: 600
    lineHeight: 1.3
    letterSpacing: "0"
  medication-title:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "17px"
    fontWeight: 600
    lineHeight: 1.3
    letterSpacing: "0"
  body:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "16px"
    fontWeight: 400
    lineHeight: 1.45
    letterSpacing: "0"
  control:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "15px"
    fontWeight: 500
    lineHeight: 1.3
    letterSpacing: "0"
  supporting:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.4
    letterSpacing: "0"
  label:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "13px"
    fontWeight: 600
    lineHeight: 1.3
    letterSpacing: "0"
  micro:
    fontFamily: '-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Helvetica Neue", Arial, sans-serif'
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1.4
    letterSpacing: "0"
rounded:
  track: "2px"
  meter: "3px"
  compact: "6px"
  field: "7px"
  surface: "8px"
  circle: "50%"
spacing:
  xs: "6px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "20px"
  2xl: "28px"
  panel: "38px"
  section: "44px"
components:
  button-primary:
    backgroundColor: "{colors.action-blue}"
    textColor: "{colors.surface-white}"
    typography: "{typography.label}"
    rounded: "{rounded.surface}"
    padding: "0 16px"
    height: "44px"
  button-primary-hover:
    backgroundColor: "{colors.action-blue-hover}"
    textColor: "{colors.surface-white}"
    rounded: "{rounded.surface}"
  button-secondary:
    backgroundColor: "{colors.surface-white}"
    textColor: "{colors.ink}"
    rounded: "{rounded.surface}"
    padding: "0 16px"
    height: "44px"
  button-wake:
    backgroundColor: "{colors.action-blue}"
    textColor: "{colors.surface-white}"
    rounded: "{rounded.surface}"
    padding: "0 24px"
    height: "56px"
  input-time:
    backgroundColor: "{colors.surface-muted}"
    textColor: "{colors.ink}"
    rounded: "{rounded.field}"
    padding: "0 12px"
    height: "46px"
  focus-panel:
    backgroundColor: "{colors.night-glass}"
    textColor: "{colors.surface-white}"
    rounded: "{rounded.surface}"
    padding: "34px 38px 46px"
  medication-card:
    backgroundColor: "{colors.light-glass}"
    textColor: "{colors.ink}"
    rounded: "{rounded.surface}"
    padding: "15px 17px 15px 18px"
---

# Design System: 药时

## Overview

**Creative North Star: "静谧床头仪表"**

**Brand Signature:** 中文品牌“药时”，英文名称与工程标识“TakeTime”。

**Slogan:** “服药有时，养正无恙”。口号表达规律与安稳，只用于品牌语境，不暗示疗效或诊疗结论。

药时的视觉系统像一台放在床头、每天醒来即可信任的私人仪表。它安静、可靠、贴近生活：先把当前唯一需要处理的用药动作放到视线中心，其余信息退居可展开的时间安排和每日总结。

这不是营销展示，也不是医院后台。深色焦点面板承担唯一的主舞台，浅色玻璃负责内容分组，晨雾灰画布提供稳定背景。空间保持克制密度，交互保留足够触控尺度，避免警报式压迫感、过度圆润和装饰性动效。

Apple-inspired 语言提供精确排版、透明材质和系统级交互的基础，但药时按中文阅读与医疗操作场景重新校准。中文保持自然字距，语义状态色不受单一蓝色限制，可读性、明确反馈和患者安全始终高于形式模仿。

**Key Characteristics:**

- 单一深色焦点面板承载当前下一步，其他内容保持安静。
- 晨雾灰画布与浅色玻璃建立克制分层，不堆叠浮动卡片。
- 行动蓝只用于可操作元素，绿、琥珀和红只表达状态。
- 7-8px 紧凑圆角、单层柔影和 44px 最小触控目标贯穿系统。
- 140-300ms 的淡入、交接与轻位移只解释状态变化，不争夺注意力。

## Colors

颜色以中性画布和黑白玻璃为主体，行动蓝建立操作路径，少量语义色明确记录、逾时和风险状态。

### Primary

- **行动蓝** (`colors.action-blue`): 主要按钮、焦点、当前时间线节点和可操作链接；悬停使用 `colors.action-blue-hover`。
- **浅面链接蓝** (`colors.link-blue`): 用于浅色表面的文字按钮和编辑入口，与实心主操作保持层级差。
- **暗面链接蓝** (`colors.dark-link-blue`): 仅用于静夜墨表面上的文字链接；悬停使用 `colors.dark-link-blue-hover`，保证深色背景对比度。

### Secondary

- **已记录绿** (`colors.recorded-green` / `colors.recorded-surface`): 浅色表面的已确认服药、完成状态和成功反馈；静夜墨表面使用更明亮的 `colors.success-on-dark`。
- **逾时琥珀** (`colors.overdue-amber`): 计划时间已过但尚未记录的状态提示。
- **风险红** (`colors.danger-red` / `colors.danger-surface`): 浅色表面的停用、错误和不可逆风险提示；静夜墨表面使用 `colors.danger-on-dark`，不用于普通提醒。

### Neutral

- **静夜墨** (`colors.night-glass`): 当前下一步和七日概览等主焦点表面。
- **晨雾灰** (`colors.canvas-mist`): 全局画布，降低长时间查看时的眩光。
- **清晰墨字** (`colors.ink`): 浅色表面的主要文字。
- **纸白表面** (`colors.surface-white`) 与 **轻雾表面** (`colors.surface-muted`): 控件、输入和静态内容层。
- **浅色玻璃** (`colors.light-glass`): 作息、时间线、计划和对话框的透明分组材料。
- **次级文字** (`colors.text-secondary`) 与 **辅助文字** (`colors.text-tertiary`): 降低信息权重，但小号文字在浅色表面仍须满足 WCAG AA；Apple 参考中的 48% 黑仅保留为非文字装饰参考，不用于本产品的可读文本。
- **轻分隔线** (`colors.divider`): 只建立结构边界，不承担文字或控件状态的唯一识别信号。

### Named Rules

**The One Action Blue Rule.** 行动蓝只标记可以操作或正在发生的内容，不把品牌色当作装饰填充。

**The Semantic Color Is Information Rule.** 绿、琥珀和红必须同时伴随文字、图标或形态，不允许只靠颜色表达用药状态。

**The Night Glass Legibility Rule.** 静夜墨上的正文和操作必须保持清晰对比；透明与模糊不能削弱药名、剂量或时间。

## Typography

**Display Font:** Apple 系统显示字体，回退至 SF Pro Display、PingFang SC、Helvetica Neue、Arial 和通用无衬线字体。

**Body Font:** Apple 系统正文字体，回退至 SF Pro Text、PingFang SC、Helvetica Neue、Arial 和通用无衬线字体。

**Brand Mark Font:** 左上角“药”字与文字 SVG 使用楷体气质的中文字体栈，优先 `Kaiti SC`、`STKaiti`、`KaiTi`，回退至通用衬线字体。它是品牌签名，不用于药名、剂量或操作文字。

**Brand Tagline Font:** “服药有时，养正无恙”使用自托管、仅含所需字形的 Noto Serif SC 500 子集，回退至 `Songti SC`、`STSong`、`SimSun` 和通用衬线字体。14px、500 字重、1.2 行高和自然字距构成克制的现代宋体气质；它只用于品牌副标题，不进入正文或药物信息。

**Character:** 系统字体让界面保持熟悉、克制和跨平台可靠；PingFang SC 为中文提供稳定字形。所有文字保持自然字距，数字时间和进度使用等宽数字特性以减少跳动。

### Hierarchy

- **Display** (600, `typography.display`, line-height 1.1): 页面标题；在 760px 以下降为 32px，在 420px 以下降为 28px。
- **Headline** (600, `typography.headline`, line-height 1.14): 焦点面板的当前动作；在窄屏降为 28px 或 26px。
- **Title** (600, `typography.title`, line-height 1.2): 区块、对话框和可展开面板标题；移动端降为 22px。
- **Metrics** (600, 52/46px desktop): 今日进度使用 `typography.metric-primary`，历史总量使用 `typography.metric-history`；各自在断点下降为 44/40px 与 38/32px，历史比率使用 40/32/28px。
- **Medication** (600, 18/17/16px): 焦点面板药名使用 `typography.medication-focus`，时间线药名使用 `typography.medication-title`，窄屏可降为正文尺寸。
- **Body / Control** (400-500, 16/15px): 主要说明与字段使用 `typography.body`，按钮与日期控件使用 `typography.control`。
- **Supporting** (400, `typography.supporting`, line-height 1.4): 剂量、补充说明、Toast 和错误反馈。
- **Label** (600, `typography.label`, line-height 1.3): 日期、kicker、状态标题和分组标签。
- **Micro** (400, `typography.micro`, line-height 1.4): 同步状态、辅助时间和次要元数据。
- **Control Glyphs** (23/25/28px): 确认、关闭和日期切换符号使用独立字号，它们服务于固定 44-46px 控件几何，不进入内容文字层级。

### Named Rules

**The Chinese Legibility Rule.** 中文与控件统一使用 `letter-spacing: 0`；不要机械套用 Apple 英文规范中的负字距。

**The Action Before Metric Rule.** 当前药物和动作标题的视觉权重必须高于完成率、计数和辅助说明。

## Layout

页面采用最大 1040px 的单一中心容器。桌面画布左右至少保留 20px，顶部导航左右至少保留 16px；主视图上方 44px、下方 80px，区块间距以 20-44px 为主。页面结构是操作界面而非营销 Hero：标题、日期切换、焦点面板、作息调整和每日总结按任务优先级垂直排列。

桌面时间字段使用五列网格，历史概览采用 38% 图像与 62% 指标分栏。760px 以下页面容器收至最大 640px、时间字段变为两列；420px 以下焦点摘要、药物动作和历史概览变为单列；359px 以下日期控制换行、时间字段变为单列。任何断点都不得产生横向滚动。

**The One Next Action Rule.** 首屏只允许一个明确的主要任务获得最高层级；完整时间线、设置和历史必须退居其后。

**The Stable Control Geometry Rule.** 日期切换、进度、图标按钮和服药操作使用稳定宽高，状态变化不得推动周围布局。

**The Shanghai Day Boundary Rule.** “今天”、起床打卡、跨日待办、记录时间和午夜自动翻日统一以 `Asia/Shanghai` 计算，不依赖访问设备的本地时区。

## Elevation & Depth

系统采用克制分层，而不是为每个区块制造漂浮感。深色焦点面板是唯一持续承担主层级的表面，浅色玻璃只负责把相关操作组织为连续区域。导航使用 20px 模糊，深色主面板使用 24px 模糊，浅色分组通常使用 18-22px 模糊。

### Shadow Vocabulary

- **环境柔影** (`0 12px 32px rgba(0, 0, 0, 0.12)`): 焦点面板、主要玻璃容器、对话框和 Toast 共用的唯一阴影；它表达材料与画布的分离，不表示可点击性。
- **半透明描边** (`1px solid rgba(255, 255, 255, 0.09)`): 深浅玻璃在复杂背景上的轮廓保护。
- **语义状态轨** (`3px solid` semantic color): 时间线药物卡左侧状态标记，与文字状态共同使用。

### Named Rules

**The Ambient Not Floating Rule.** 阴影负责提供环境深度，不允许通过多层阴影、发光或大幅悬浮制造装饰性层级。

**The Glass Must Group Rule.** 毛玻璃必须承担信息分组或焦点职责；没有结构作用的玻璃效果应被移除。

## Shapes

矩形表面保持紧凑、可预期的轻微曲线。主要按钮、焦点面板、玻璃容器和卡片使用 `rounded.surface`；时间字段使用 `rounded.field`；错误操作等紧凑控件可使用 `rounded.compact`。进度轨使用 2-3px 微圆角，时间线节点和服药确认控件使用正圆。

系统不使用胶囊按钮，也不把大圆角当作亲和力捷径。矩形组件不得超过 8px 圆角；只有真实圆形控件使用 50%。

**The Compact Curves Rule.** 轻微圆角传达可触控与精度，大面积圆润、嵌套卡片和无功能胶囊均不属于药时。

## Components

组件整体克制可靠，关键动作有触感。所有交互目标至少 44px，获得键盘焦点时显示 2px 行动蓝轮廓和 2px 外偏移；按压反馈为向下 1px，悬停抬升仅在支持 hover 的设备上使用。

### Buttons

- **Shape:** 轻微圆角（8px），常规高度 44px；起床按钮高度 56px。
- **Primary:** 行动蓝底、白字、15px/500；起床按钮使用 17px/600 并占满可用宽度。
- **Hover / Focus:** 悬停切换到行动蓝高亮色，按压下移 1px；焦点始终使用可见轮廓。
- **Secondary / Text / Danger:** 次级按钮使用白色表面与轻分隔线，文字操作保持透明背景，危险操作使用风险红和淡红表面。

### Cards / Containers

- **Corner Style:** 统一 8px，不在页面区块内嵌套装饰卡片。
- **Background:** 主焦点使用静夜墨，连续内容使用浅色玻璃，画布保持晨雾灰。
- **Shadow Strategy:** 只使用环境柔影；列表内部依靠间距、分隔线和状态轨组织。
- **Border:** 玻璃可使用 1px 半透明轮廓，普通白色控件只在需要边界时使用轻分隔线。
- **Internal Padding:** 焦点面板桌面为 34px 38px 46px，移动端收至 24px 18px 40px；药物卡保持约 15-18px。

### Inputs / Fields

- **Style:** 46px 高、7px 圆角、轻雾表面和 12px 横向内边距；标签在字段上方保持清晰关联。
- **Focus:** 使用全局 2px 行动蓝焦点轮廓，不以阴影替代。
- **Error / Disabled:** 禁用状态降低透明度并显示不可用光标；错误通过持久错误条与文字说明表达。

### Navigation

顶部导航固定为 52px 的深色玻璃层，使用三列布局保持品牌、视图标签和同步状态稳定。桌面端在“药”字标记后使用“服药有时，养正无恙”作为安静的品牌副标题，不重复显示产品名；视图标签至少 44px 高，当前项以白字和 2px 下划线表示。移动端隐藏导航内的品牌副标题与同步文字，但保留品牌标记和完整操作尺寸；同一句品牌副标题在页面正常文档流的深色底部收尾区居中呈现，并为设备底部安全区留白，不使用固定悬浮。品牌“药”字的首次落印与点击按压使用相互独立的动画层，落印只执行一次，点击反馈不改变透明度或模糊，也不循环播放。

日期控件始终为“前一天、日期、后一天、回到今天”保留固定网格位置；当天状态只隐藏“回到今天”的内容，不改变日期切换器的横向或纵向位置。320px 最窄支持宽度仍保持同一行，只压缩中间日期轨的可用宽度。

### Focus Panel

焦点面板是系统签名组件。它按“未起床、跨日待办、当前动作、今日摘要、全部完成”切换内容，但外部几何保持稳定。面板优先展示当前药物、剂量、要求和唯一记录按钮；完成计数是辅助信息，进度轨位于底部。实际执行日存在尚未确认的跨日药物时，它必须先于纯起床空状态出现，同时保留“我起床了”入口，并以文字标明原计划日期。逾时项可以使用琥珀状态，但不得把整个面板变成警报色。

### Medication Timeline

时间线按计划时间分组，以 1px 竖轨、圆形节点和连续药物卡表达顺序。当前节点使用行动蓝；已服、即将服用和逾时分别使用绿色、蓝色和琥珀色左侧状态轨，同时提供文字状态。圆形确认控件完成后变为绿色勾选状态。

作息时间字段必须常驻显示相对计划日的日期标签；时钟顺序回绕时依次标为“次日”或“N日后”。跨日保存使用原生确认对话框，按实际日期列出尚未记录且受影响的药物，并明确说明已记录项目保持原计划时间。跨日药物在统计和持久化上仍属于原计划日，但在实际执行日进入首页焦点；界面与请求都以“原计划日期 + 药物 ID”识别它，不能只依赖药物 ID。

### Dialogs and Feedback

添加或编辑用药使用原生对话框语义、最大 620px 宽的浅色玻璃和模糊遮罩。Toast 提供短暂成功反馈，持久错误条提供重试入口，同步状态在导航中低调显示；任何反馈都不得遮挡当前主要操作。服药按钮在请求期间保持原有几何尺寸，并在焦点区与时间线同步显示进行中状态；成功反馈保留服务端记录时间和短时撤销，失败在原药物旁提供重试。网络结果不确定时不得断言记录失败。键盘触发动作后，焦点必须跟随到进行中状态、撤销或重试控件；有焦点的限时反馈不得被自动移除。

## Motion

动效延续 Apple-inspired 的系统感，并参考 [Ant Motion](https://motion.ant.design/index-cn) 的自然、高效与克制原则。实现保持原生 CSS、Web Animations API 与 View Transitions API 渐进增强，不为现有运行时能清晰表达的效果增加依赖。

- **Normal:** 顶部导航、日期箭头、焦点和触控几何保持稳定，不因内容切换重新入场。
- **Receding:** 旧视图或旧状态在 100-150ms 内快速退场，使用 ease-in，不阻塞数据请求。
- **Adding:** 新视图和新的下一步在 200-300ms 内自然减速进入；列表队列总延迟不得超过 120ms。
- **Direction:** 今日、记录和计划视图使用与信息顺序一致的 8-12px 水平关系；高频日期交接收敛为 5-7px，记录、撤销和起床交接使用更短的局部状态变化。
- **Interruption:** 新交互可以跳过仍在运行的过渡；动效不得延长写入锁、替代后端确认或造成重复操作。
- **Date Handoff:** 日期请求期间保留已确认的日期与当日内容，只暂停内容区写操作；最新响应成功后，当前文字在约 110ms 内完全退出，再原子渲染新日期。标题、焦点内容、作息摘要和每日总结以 220ms 减速曲线、5-7px 方向位移和不超过 96ms 的短队列依次进入；焦点面板、作息卡、总结卡与日期箭头的外框始终保持稳定。交接期间禁止克隆或叠放第二份文字和玻璃材质。连续点击始终以最后目标日期为基准，过期响应不得提交或播放动效。最终日期通过礼貌级状态区向读屏用户播报；从其他视图回到今天时先完成只读预取，再由顶层视图过渡一次性交接。
- **Reduced Motion:** `prefers-reduced-motion` 下停用位移、模糊、循环和 View Transition 伪元素动画，但保留即时状态与焦点更新。

**The Motion Must Explain Rule.** 每个动效必须说明反馈、状态或空间关系；删除后不损失含义的装饰性动画应被移除。

**The One Authored Moment Rule.** 起床后的下一步交接与品牌落印可以拥有明确性格，其余高频动效必须更安静、更快。

## Do's and Don'ts

### Do:

- **Do** 让当前唯一需要处理的药物和动作先于统计数字出现。
- **Do** 使用行动蓝标记操作，并使用文字加形态说明成功、逾时和风险状态。
- **Do** 保持 44px 最小触控目标、清晰焦点和系统字体缩放下的内容完整。
- **Do** 让毛玻璃服务于焦点或分组，并在复杂背景上保持药名、剂量和时间可读。
- **Do** 将动效限制在约 200ms 的状态确认，并完整支持 `prefers-reduced-motion`。

### Don't:

- **Don't** 把首页做成营销 Hero、医院后台或需要用户自行解读的密集仪表盘。
- **Don't** 使用渐变、发光、多层阴影、装饰纹理或超过 8px 的矩形圆角。
- **Don't** 用纯颜色表达医疗状态，也不要把风险红用于普通提醒。
- **Don't** 为每一项内容创建独立浮动卡片，或把卡片嵌套在其他卡片中。
- **Don't** 对中文应用负字距，也不要让大号统计数字压过当前用药动作。
- **Don't** 用循环、弹跳或持续运动吸引注意力；动效必须解释状态变化。
